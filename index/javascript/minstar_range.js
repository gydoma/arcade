


/*



const slider = document.getElementById("min-star");
const range = slider.max - slider.min;
const value = slider.value - slider.min;
const rangePercentage = (value / range) * 100;
slider.style.setProperty("--range-percentage", `${rangePercentage}%`);

slider.addEventListener("input", function() {
  const value = slider.value - slider.min;
  const rangePercentage = (value / range) * 100;
  slider.style.setProperty("--range-percentage", `${rangePercentage}%`);
});


*/